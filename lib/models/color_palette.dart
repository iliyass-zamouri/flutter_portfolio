import 'package:flutter/material.dart';

class ColorPalette {
  final Color mainColor;
  final Color secondColor;
  final Color thirdColor;
  final Color backgroundColor;

  const ColorPalette(
      this.mainColor, this.secondColor, this.thirdColor, this.backgroundColor);
}
